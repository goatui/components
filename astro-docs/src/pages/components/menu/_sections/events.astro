---
import Highlighter from '../../../../components/Highlighter.astro';
---
<goat-card>
  <goat-card-content class='no-padding'>
    <Highlighter lang={'javascript'} is:raw> const $list = document.querySelector('.menu');
      $list.addEventListener('goat-menu-item--click',
      function(evt) {
       console.log('<pre>' + JSON.stringify(evt.detail.value, null, 4) + '</pre>');
      });
    </Highlighter>
  </goat-card-content>
</goat-card>
