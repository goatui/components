---
import Highlighter from '../../../../components/Highlighter.astro';
---
<goat-card>
  <goat-card-content>
    <goat-select id="select" placeholder="Select" clearable style="width: 20rem"></goat-select>
    <goat-select id="select2" class='no-border' placeholder="Select" style="width: 20rem"></goat-select>
    <script is:inline>
      let $dropDown = document.getElementById('select');
      $dropDown.items = [
        { label: 'New', value: 'new', icon: 'file-earmark' },
        { label: 'Save', value: 'save', icon: 'person-circle' },
        { label: 'Save As', value: 'save-as', icon: 'person-fill' },
        {
          label: 'Nadine Brooks',
          value: 'nadine',
          icon: 'person-check-fill',
        },
        {
          label: 'Laura Ballard',
          value: 'laura',
          icon: 'file-earmark-person',
        },
      ];
      $dropDown.addEventListener('goat:change', function (evt) {
        myConsole.log(JSON.stringify(evt.detail, null, 4));
      });

      $dropDown = document.getElementById('select2');
      $dropDown.items = [
        { label: 'New', value: 'new' },
        { label: 'Save', value: 'save' },
        { label: 'Save As', value: 'save-as'},
        {
          label: 'Nadine Brooks',
          value: 'nadine'
        },
        {
          label: 'Laura Ballard',
          value: 'laura'
        },
      ];
    </script>
  </goat-card-content>
  <goat-card-content class='no-padding'>
    <Highlighter lang={'html'}>
    <goat-select id="select" placeholder="Select" clearable style="width: 20rem"></goat-select>
<goat-select id="select2" class='no-border' placeholder="Select"  style="width: 20rem"></goat-select>
    </Highlighter>
  </goat-card-content>
  <goat-card-content class='no-padding'>
    <Highlighter lang={'javascript'} is:raw>
const $dropDown = document.getElementById('select');
$dropDown.items = [
  { label: 'New', value: 'new', icon: 'file-earmark' },
  { label: 'Save', value: 'save', icon: 'person-circle' },
  { label: 'Save As', value: 'save-as', icon: 'person-fill' },
  {
    label: 'Nadine Brooks',
    value: 'nadine',
    icon: 'person-check-fill',
  },
  {
    label: 'Laura Ballard',
    value: 'laura',
    icon: 'file-earmark-person',
  },
];
$dropDown.addEventListener('goat:change', function (evt) {
  myConsole.log(JSON.stringify(evt.detail, null, 4));
});
    </Highlighter>
  </goat-card-content>
</goat-card>
